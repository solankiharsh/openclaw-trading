# Server
PORT=3001
NODE_ENV=development

# Privy (Required)
PRIVY_APP_ID=your-privy-app-id
PRIVY_APP_SECRET=your-privy-app-secret

# JWT (Required)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# PostgreSQL - Railway (Required)
DATABASE_URL=postgresql://postgres:password@containers-us-west-xxx.railway.app:5432/railway

# Redis/Upstash (Optional - uses in-memory store if not set)
# REDIS_URL=redis://default:xxx@xxx.upstash.io:6379

# Replica role controls (for multi-replica safety)
# Single replica default: keep all true.
ENABLE_BACKGROUND_WORKERS=true
ENABLE_WEBHOOK_WORKER=true
ENABLE_SORTINO_CRON=true
ENABLE_HELIUS_MONITOR=true
ENABLE_CHAIN_MONITORS=true
REPLICA_ID=replica-1

# Webhook queue config
# WEBHOOK_QUEUE_REDIS_URL=redis://default:xxx@xxx.upstash.io:6379
# WEBHOOK_QUEUE_NAME=supermolt:solana:webhooks
# WEBHOOK_QUEUE_CONCURRENCY=4
# WEBHOOK_QUEUE_MAX=2000

# Cron lock TTL (ms)
# SORTINO_LOCK_TTL_MS=3500000

# DevPrint Trading Backend (Optional - for AI trading features)
# DEVPRINT_URL=http://localhost:8080
# DEVPRINT_WS_URL=ws://localhost:8080/ws

# Internal API Key (Required for DevPrint → SR-Mobile trade creation)
# INTERNAL_API_KEY=generate-a-random-32-char-string

# Solana Network Config (Devnet for USDC Hackathon)
SOLANA_NETWORK=devnet
SOLANA_RPC_URL=https://api.devnet.solana.com

# USDC Configuration (Devnet)
USDC_MINT=4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU

# Treasury Wallet (Devnet - for USDC distribution)
TREASURY_PRIVATE_KEY=<base64_encoded_private_key>

# BSC (Binance Smart Chain) Configuration
BSC_RPC_URL=https://bsc-dataseed.binance.org
# USDC Contract (Mainnet: 0x8AC76a51cc950d9822D68b83fE1Ad97B32Cd580d)
# USDC Contract (Testnet: 0x64544969ed7EBf5f083679233325356EbE738930)
BSC_REWARD_TOKEN_ADDRESS=0x64544969ed7EBf5f083679233325356EbE738930
BSC_TREASURY_PRIVATE_KEY=<0x_prefixed_private_key>

# Agent Simulator (Devnet scanner wallets)
AGENT_ALPHA_PRIVATE_KEY=<base64_encoded_private_key>
# Alpha Scanner (Devnet): FwhhaoXG67kQiAG7P2siN6HPvbyQ49E799uxcmnez5qk

# Etherscan V2 API — one key for all EVM chains (free at etherscan.io)
# Works for BSC, ETH, Base, Arbitrum, etc. via chainid param
ETHERSCAN_API_KEY=your-etherscan-api-key

# BSC Integration
BSC_RPC_URL=https://bsc-dataseed1.binance.org/
BSC_TREASURY_PRIVATE_KEY=0x...  # Signs Four.Meme auth + pays gas

# Four.Meme (BSC Token Launcher)
# FOURMEME_API_URL=https://four.meme/meme-api/v1  # default, can override

# Pump.fun (Solana Token Launcher)
# SOLANA_DEPLOYER_PRIVATE_KEY=<base58_solana_keypair>  # For pump.fun token creation
# Falls back to TREASURY_PRIVATE_KEY (base64) if not set
# Wallet needs ~0.03 SOL for gas on mainnet
